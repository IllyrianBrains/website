# Illyrian Brains — website (Astro)

# Reference material, scratch content, and any nested checkouts used while
# migrating content from the forum. Never meant to be published.
dev/

# Dependencies
node_modules/

# Build output
dist/
.astro/

# Member directory is fetched fresh from a private Google Sheet at dev/build
# time (see scripts/sync-members-sheet.mjs) rather than committed — don't
# store member data in the repo.
src/data/members.json
scripts/members-seed.csv

# Env / editor / OS noise
*.env
.env
.DS_Store
.vscode/
.idea/
*.log
